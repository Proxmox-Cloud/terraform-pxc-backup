# access-namespace module

This module can be standalone deployed if your goal is only to restore backups from the [pve-cloud-backup server](https://github.com/Proxmox-Cloud/pve-cloud-backup) via its `brctl` cli tool.